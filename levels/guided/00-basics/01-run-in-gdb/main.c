#include <stdio.h>

int main(void)
{
    puts("+------------------------------------------------------------+");
    puts("| RUN IN GDB                                                 |");
    puts("+------------------------------------------------------------+");
    puts("");
    puts("This program can be run from inside GDB.");
    puts("");
    puts("Debugger run status:");
    puts("  RUNNING");

    return 0;
}
